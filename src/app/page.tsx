import React from "react";
import LandingPage from "@/components/landingPage"; // Import your LandingPage component

const HomePage = () => {
  return (
    <LandingPage initialPosts={[]} /> // Use the LandingPage component in your JSX
  );
};

export default HomePage;
